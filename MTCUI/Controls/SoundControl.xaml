<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MTCUI.Controls.SoundControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MTCUI.Controls"
    xmlns:m="using:MTCUI.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Border Width="180" Margin="0 10 5 0" Style="{StaticResource PanelStyle}" >
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="1"  Orientation="Vertical" Margin="0 0 0 10">
                    <StackPanel Orientation="Horizontal" Margin="15 5 0 0">
                        <Button Command="{x:Bind SoundVM.PlaySoundCommand}"
                                CommandParameter="{x:Bind SoundVM.Sound1}">
                            <FontIcon Glyph="{x:Bind SoundVM.Sound1.Glyph, Mode=OneWay}"/>
                        </Button>
                        <TextBlock Text="Внимание" Margin="10 0 0 0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="15 5 0 0">
                        <Button Command="{x:Bind SoundVM.PlaySoundCommand}"
                                CommandParameter="{x:Bind SoundVM.Sound2}">
                            <FontIcon Glyph="{x:Bind SoundVM.Sound2.Glyph, Mode=OneWay}"/>
                        </Button>
                        <TextBlock Text="Огън"  Margin="10 0 0 0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="15 5 0 0">
                        <Button Command="{x:Bind SoundVM.PlaySoundCommand}"
                                CommandParameter="{x:Bind SoundVM.Sound3}">
                            <FontIcon Glyph="{x:Bind SoundVM.Sound3.Glyph, Mode=OneWay}"/>
                        </Button>
                        <TextBlock Text="Отбой"  Margin="10 0 0 0" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
