<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MTCUI.Controls.TimerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MTCUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Border Width="180" Margin="0 10 5 0" Style="{StaticResource PanelStyle}" >
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="40"/>
                </Grid.RowDefinitions>
                <TextBlock HorizontalAlignment="Center" Grid.Row="0" 
                           Text="{x:Bind TimerVM.TimerText, Mode=OneWay}" 
                           FontSize="32"/>
                
                <StackPanel Grid.Row="1" Orientation="Horizontal" 
                            HorizontalAlignment="Center" Margin="0 0 0 5">
                    <Button Width="80" Height="30" Content="{x:Bind TimerVM.StartStopText, Mode=OneWay}" 
                            Style="{StaticResource PrimaryButtonStyle}"
                            Margin="10 0 10 0" 
                            Command="{x:Bind TimerVM.StartStopCommand}"/>
                    <Button Width="80" Height="30" Content="НУЛИРАНЕ" Margin="0 0 10 0" 
                            Style="{StaticResource PrimaryButtonStyle}"
                            Command="{x:Bind TimerVM.ResetCommand}" />
                    
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
