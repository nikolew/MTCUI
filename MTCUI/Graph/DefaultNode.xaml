<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MTCUI.Graph">


    <ControlTemplate x:Key="__NodeTemplate__" TargetType="local:DefaultNode">
        <Grid>
            <Border x:Name="NodeBorder"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    Background="{TemplateBinding Background}"
                    CornerRadius="4" >
            </Border>

            <ContentPresenter Content="{TemplateBinding Content}"
                              ContentTemplateSelector="{Binding TargetTemplateSelector,
                              RelativeSource={RelativeSource TemplatedParent}}">
                
            </ContentPresenter>
            
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup x:Name="SelectionStates">
                    <VisualState x:Name="Unselected"/>
                    <VisualState x:Name="Selected">
                        <VisualState.Setters>
                            <Setter Target="NodeBorder.BorderBrush" Value="Orange"/>
                            <Setter Target="NodeBorder.BorderThickness" Value="1"/>
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
            

        </Grid>

    </ControlTemplate>
    
</ResourceDictionary>
